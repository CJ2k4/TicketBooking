services:
  db:
    image: postgres:16-alpine
    container_name: seatbooking-sb
    environment:
      POSTGRES_DB: seatbooking
      POSTGRES_USER: seat
      POSTGRES_PASSWORD: seat
    ports:
      - "5432:5432"
    volumes:
      - seatdata:/var/lib/postgresql/data

volumes:
  seatdata:
